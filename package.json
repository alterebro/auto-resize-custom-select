{
  "name": "auto-resize-custom-select",
  "version": "0.1.0",
  "description": "Auto width resized and custom select html elements",
  "main": "src/customSelect.js",
  "module": "src/customSelect.js",
  "browser": "dist/customSelect.min.js",
  "unpkg": "dist/customSelect.min.js",
  "scripts": {
    "clean": "rm -rf dist/*",
    "build": "npm run clean && gulp",
    "version": "npm run build && git add -A dist/",
    "postversion": "git push && git push --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alterebro/auto-resize-custom-select.git"
  },
  "keywords": [
    "select",
    "custom",
    "autoresize"
  ],
  "author": "Jorge Moreno @alterebro (https://moro.es)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/alterebro/auto-resize-custom-select/issues"
  },
  "homepage": "https://github.com/alterebro/auto-resize-custom-select#readme",
  "devDependencies": {
    "@babel/core": "^7.12.13",
    "@babel/preset-env": "^7.12.13",
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-header": "^2.0.9",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  }
}
